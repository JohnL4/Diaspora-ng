/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {ClusterSerializerDot} from './cluster-serializer-dot';

describe('ClusterSerializerDot', () => {
  it('should create an instance', () => {
    expect(new ClusterSerializerDot()).toBeTruthy();
  });
});
